/**
 * PostCSS configuration for the project.  This file defines the
 * plugins used to process CSS.  Tailwind CSS and Autoprefixer are
 * configured to generate utility classes and vendor prefixes.
 */
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
